import './style.css'

const NumberHistoric = ({children}) => {
    return(
        <div className='container-result'>
            <span>{children}</span>
        </div>
    )
}

export default NumberHistoric